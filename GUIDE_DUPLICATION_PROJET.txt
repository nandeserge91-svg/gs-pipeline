╔══════════════════════════════════════════════════════════════════════════════╗
║                                                                              ║
║         📦 GUIDE COMPLET - DUPLIQUER UN PROJET SUR AUTRE ORDINATEUR          ║
║                 Avec nouveaux comptes GitHub, Railway & Vercel               ║
║                                                                              ║
║                          Version 1.0 - 1er Février 2026                      ║
║                                                                              ║
╚══════════════════════════════════════════════════════════════════════════════╝


═══════════════════════════════════════════════════════════════════════════════
                              🎯 OBJECTIF
═══════════════════════════════════════════════════════════════════════════════

Ce guide vous permet de dupliquer INTÉGRALEMENT votre projet actuel sur un 
autre ordinateur, avec de nouveaux comptes pour :
  ✅ GitHub (nouveau dépôt, nouveau compte)
  ✅ Railway (nouvelle base de données, nouveau compte)
  ✅ Vercel (nouveau déploiement frontend, nouveau compte)

Temps estimé : 30-45 minutes


═══════════════════════════════════════════════════════════════════════════════
                          📋 CE QUE VOUS DEVEZ AVOIR
═══════════════════════════════════════════════════════════════════════════════

Sur l'ORDINATEUR SOURCE (actuel) :
  ✓ Votre projet fonctionnel
  ✓ Accès au code source complet
  ✓ Liste de vos variables d'environnement (.env)
  ✓ Base de données avec données (optionnel : si vous voulez les transférer)

Sur le NOUVEL ORDINATEUR :
  ✓ Node.js 18+ installé (https://nodejs.org)
  ✓ Git installé (https://git-scm.com)
  ✓ Un éditeur de code (VS Code, Cursor, etc.)
  ✓ Connexion internet stable

Nouveaux comptes à créer (GRATUITS) :
  ✓ GitHub - https://github.com/signup
  ✓ Railway - https://railway.app
  ✓ Vercel - https://vercel.com/signup


═══════════════════════════════════════════════════════════════════════════════
                    🚀 MÉTHODE COMPLÈTE - ÉTAPE PAR ÉTAPE
═══════════════════════════════════════════════════════════════════════════════

┌──────────────────────────────────────────────────────────────────────────────┐
│ PHASE 1 : PRÉPARER LE PROJET SOURCE (Ordinateur actuel - 5 minutes)          │
└──────────────────────────────────────────────────────────────────────────────┘

1.1. NETTOYER LE PROJET
───────────────────────

Ouvrir un terminal dans votre projet actuel :

```bash
# Supprimer node_modules (on réinstallera sur le nouvel ordinateur)
rm -rf node_modules
rm -rf frontend/node_modules

# Supprimer les fichiers de build
rm -rf dist
rm -rf frontend/dist
rm -rf frontend/build

# Supprimer les fichiers de cache
rm -rf .next
rm -rf .turbo
```

⚠️ **IMPORTANT** : Ne supprimez PAS :
  - package.json
  - package-lock.json
  - prisma/schema.prisma
  - Vos fichiers source (.js, .jsx, .ts, .tsx)
  - Vos fichiers de configuration


1.2. SAUVEGARDER LES VARIABLES D'ENVIRONNEMENT
───────────────────────────────────────────────

Créez un fichier temporaire pour noter TOUTES vos variables :

**Fichier : ENV_BACKUP.txt**

```env
# ===== BACKEND =====
DATABASE_URL=votre_url_railway
JWT_SECRET=votre_secret
PORT=3000

# ===== FRONTEND =====
VITE_API_URL=http://localhost:3000

# ===== AUTRES =====
NODE_ENV=development
# ... toutes vos autres variables
```

💡 **Astuce** : Copiez le contenu de vos fichiers :
  - backend/.env
  - frontend/.env
  - .env (racine si présent)


1.3. EXPORTER LA BASE DE DONNÉES (OPTIONNEL)
─────────────────────────────────────────────

Si vous voulez transférer les données :

```bash
# Méthode 1 : Export Prisma
npx prisma db pull
# Cela sauvegarde le schéma

# Méthode 2 : Export SQL (si PostgreSQL)
# Depuis Railway :
# 1. Aller sur railway.app
# 2. Votre projet > Database > Data
# 3. Bouton "Export" ou utiliser pg_dump
```

💡 **Alternative** : Repartir avec une DB vide (recommandé pour un nouveau projet)


1.4. CRÉER UNE ARCHIVE DU PROJET
─────────────────────────────────

```bash
# Remonter d'un niveau au-dessus de votre projet
cd ..

# Créer une archive ZIP (Windows : utiliser l'explorateur de fichiers)
# Mac/Linux :
zip -r mon-projet-backup.zip mon-projet/ -x "*/node_modules/*" "*/dist/*" "*/.git/*"

# Ou avec tar :
tar -czf mon-projet-backup.tar.gz mon-projet/ --exclude=node_modules --exclude=dist --exclude=.git
```

📦 **Résultat** : Vous avez maintenant un fichier .zip ou .tar.gz à transférer


1.5. TRANSFÉRER SUR LE NOUVEL ORDINATEUR
─────────────────────────────────────────

Méthodes possibles :
  a) **Clé USB** : Copier le fichier .zip
  b) **Cloud** : Google Drive, Dropbox, OneDrive
  c) **Email** : Si le fichier est petit (<25 MB)
  d) **WeTransfer** : https://wetransfer.com (gratuit jusqu'à 2 GB)
  e) **GitHub** : Pousser sur un dépôt privé temporaire


┌──────────────────────────────────────────────────────────────────────────────┐
│ PHASE 2 : CONFIGURER LE NOUVEL ORDINATEUR (10 minutes)                       │
└──────────────────────────────────────────────────────────────────────────────┘

2.1. INSTALLER LES OUTILS NÉCESSAIRES
──────────────────────────────────────

**Node.js** (si pas déjà installé) :
```bash
# Vérifier la version
node --version
npm --version

# Si pas installé : https://nodejs.org
# Télécharger et installer la version LTS
```

**Git** (si pas déjà installé) :
```bash
# Vérifier
git --version

# Si pas installé : https://git-scm.com
# Télécharger et installer
```

**Configurer Git** (avec votre NOUVEAU compte) :
```bash
git config --global user.name "Votre Nouveau Nom"
git config --global user.email "nouveau.email@example.com"
```


2.2. EXTRAIRE LE PROJET
───────────────────────

```bash
# Créer un dossier de travail
mkdir C:\MesProjets
cd C:\MesProjets

# Extraire l'archive
# Windows : Clic-droit > Extraire tout
# Mac/Linux :
unzip mon-projet-backup.zip
# ou
tar -xzf mon-projet-backup.tar.gz

# Entrer dans le projet
cd mon-projet
```


2.3. INSTALLER LES DÉPENDANCES
───────────────────────────────

```bash
# Backend
npm install

# Frontend (si séparé)
cd frontend
npm install
cd ..
```

⏱️ Cela peut prendre 2-5 minutes selon votre connexion


┌──────────────────────────────────────────────────────────────────────────────┐
│ PHASE 3 : CRÉER UN NOUVEAU DÉPÔT GITHUB (5 minutes)                          │
└──────────────────────────────────────────────────────────────────────────────┘

3.1. CRÉER UN NOUVEAU COMPTE GITHUB (si nécessaire)
────────────────────────────────────────────────────

1. Aller sur https://github.com/signup
2. Entrer votre nouvel email
3. Créer un mot de passe fort
4. Choisir un username
5. Vérifier l'email


3.2. CRÉER UN NOUVEAU DÉPÔT
───────────────────────────

1. Cliquer sur "+" en haut à droite > "New repository"
2. Remplir :
   - Repository name : `mon-nouveau-projet`
   - Description : "Mon projet dupliqué"
   - Visibilité : **Private** (recommandé) ou Public
   - ❌ **NE PAS** cocher "Add README" (vous avez déjà le code)
   - ❌ **NE PAS** ajouter .gitignore (vous l'avez déjà)
3. Cliquer "Create repository"

📝 **Note** : GitHub affichera les commandes à exécuter


3.3. CONNECTER VOTRE PROJET AU NOUVEAU DÉPÔT
─────────────────────────────────────────────

Dans le terminal, depuis votre projet :

```bash
# Initialiser Git (si pas déjà fait)
git init

# Vérifier le .gitignore
# Créer .gitignore si absent :
cat > .gitignore << EOF
node_modules/
dist/
.env
.env.local
.env.production
*.log
.DS_Store
EOF

# Ajouter tous les fichiers
git add .

# Premier commit
git commit -m "Initial commit - Project duplicated"

# Connecter au nouveau dépôt GitHub
git remote add origin https://github.com/VOTRE_NOUVEAU_USERNAME/mon-nouveau-projet.git

# Pousser le code
git branch -M main
git push -u origin main
```

🔐 **Authentification GitHub** :
  - GitHub peut demander vos identifiants
  - Utilisez un **Personal Access Token** :
    1. GitHub > Settings > Developer settings > Personal access tokens > Tokens (classic)
    2. Generate new token (classic)
    3. Cocher "repo"
    4. Générer et copier le token
    5. Utiliser le token comme mot de passe lors du push


3.4. VÉRIFIER
─────────────

Aller sur `https://github.com/VOTRE_NOUVEAU_USERNAME/mon-nouveau-projet`
✅ Vous devriez voir tous vos fichiers !


┌──────────────────────────────────────────────────────────────────────────────┐
│ PHASE 4 : CRÉER UNE NOUVELLE BASE DE DONNÉES SUR RAILWAY (10 minutes)        │
└──────────────────────────────────────────────────────────────────────────────┘

4.1. CRÉER UN NOUVEAU COMPTE RAILWAY (si nécessaire)
─────────────────────────────────────────────────────

1. Aller sur https://railway.app
2. Cliquer "Start a New Project"
3. Se connecter avec :
   - Email (nouveau)
   - Ou GitHub (nouveau compte)
   - Ou Google


4.2. CRÉER UN NOUVEAU PROJET
────────────────────────────

1. Cliquer "+ New Project"
2. Choisir "Provision PostgreSQL"
3. Railway crée automatiquement une base de données

⏱️ Attendre 30 secondes que la DB soit créée


4.3. RÉCUPÉRER LA DATABASE_URL
───────────────────────────────

1. Cliquer sur votre Database
2. Onglet "Connect"
3. Section "Available Variables"
4. Copier la valeur de **DATABASE_URL**

Format :
```
postgresql://postgres:MOT_DE_PASSE@mondomaine.railway.app:5432/railway
```

💡 **Sauvegarder cette URL** dans un fichier temporaire !


4.4. CONFIGURER LE .env BACKEND
────────────────────────────────

Dans votre projet, créer/modifier `backend/.env` ou `.env` :

```env
# Database
DATABASE_URL="postgresql://postgres:xxxxxxxx@xxx.railway.app:5432/railway"

# JWT
JWT_SECRET="votre_nouveau_secret_jwt_super_long_et_securise"

# Server
PORT=3000
NODE_ENV=development

# ... autres variables
```

💡 **Générer un nouveau JWT_SECRET** :
```bash
# Dans le terminal
node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
```


4.5. APPLIQUER LES MIGRATIONS PRISMA
─────────────────────────────────────

```bash
# Depuis la racine du projet (ou backend/)
npx prisma generate
npx prisma migrate deploy

# Ou si première fois :
npx prisma migrate dev --name init

# Vérifier avec Prisma Studio (optionnel)
npx prisma studio
# Ouvre http://localhost:5555
```

✅ Votre nouvelle base de données est prête !


4.6. IMPORTER LES DONNÉES (OPTIONNEL)
──────────────────────────────────────

Si vous avez exporté les données depuis l'ancienne DB :

**Méthode 1 : Seed Prisma**
```bash
# Créer prisma/seed.js avec vos données
npx prisma db seed
```

**Méthode 2 : Import SQL**
```bash
# Si vous avez un fichier dump.sql
psql $DATABASE_URL < dump.sql
```

**Méthode 3 : Manuellement**
  - Ouvrir Prisma Studio : `npx prisma studio`
  - Créer les enregistrements un par un


4.7. CRÉER UN UTILISATEUR ADMIN (Important !)
──────────────────────────────────────────────

Si votre projet a un système d'authentification :

**Option A : Via script**
```bash
# Créer scripts/create-admin.js
```

```javascript
import { PrismaClient } from '@prisma/client';
import bcrypt from 'bcryptjs';

const prisma = new PrismaClient();

async function createAdmin() {
  const hashedPassword = await bcrypt.hash('admin123', 10);
  
  const admin = await prisma.user.create({
    data: {
      email: 'admin@example.com',
      nom: 'Admin',
      prenom: 'Super',
      password: hashedPassword,
      role: 'ADMIN',
      telephone: '0000000000',
    },
  });
  
  console.log('✅ Admin créé:', admin);
}

createAdmin()
  .then(() => prisma.$disconnect())
  .catch((e) => {
    console.error(e);
    prisma.$disconnect();
  });
```

```bash
node scripts/create-admin.js
```

**Option B : Via Prisma Studio**
```bash
npx prisma studio
# Créer manuellement un utilisateur admin
```


┌──────────────────────────────────────────────────────────────────────────────┐
│ PHASE 5 : DÉPLOYER LE BACKEND SUR RAILWAY (5 minutes)                        │
└──────────────────────────────────────────────────────────────────────────────┘

5.1. CRÉER UN SERVICE BACKEND
──────────────────────────────

1. Dans votre projet Railway, cliquer "+ New"
2. Choisir "GitHub Repo"
3. Connecter votre nouveau compte GitHub (autoriser Railway)
4. Sélectionner le dépôt `mon-nouveau-projet`


5.2. CONFIGURER LE SERVICE
───────────────────────────

1. Aller dans Settings du service backend
2. **Root Directory** : Mettre `backend` si votre backend est dans un sous-dossier
3. **Build Command** : Laisser vide (ou `npm install`)
4. **Start Command** : `npm start` ou `node server.js`


5.3. AJOUTER LES VARIABLES D'ENVIRONNEMENT
───────────────────────────────────────────

1. Onglet "Variables"
2. Cliquer "+ New Variable"
3. Ajouter :

```env
DATABASE_URL=${{Postgres.DATABASE_URL}}
JWT_SECRET=votre_secret_jwt
NODE_ENV=production
PORT=3000
```

💡 **Astuce** : `${{Postgres.DATABASE_URL}}` récupère automatiquement l'URL de votre DB


5.4. OBTENIR L'URL DU BACKEND
──────────────────────────────

1. Onglet "Settings"
2. Section "Domains"
3. Cliquer "Generate Domain"
4. Railway génère une URL : `https://votre-backend.up.railway.app`

📝 **Sauvegarder cette URL** !


5.5. TESTER LE BACKEND
──────────────────────

```bash
# Tester avec curl ou Postman
curl https://votre-backend.up.railway.app/api/health

# Ou ouvrir dans le navigateur
https://votre-backend.up.railway.app
```


┌──────────────────────────────────────────────────────────────────────────────┐
│ PHASE 6 : DÉPLOYER LE FRONTEND SUR VERCEL (5 minutes)                        │
└──────────────────────────────────────────────────────────────────────────────┘

6.1. CRÉER UN NOUVEAU COMPTE VERCEL (si nécessaire)
────────────────────────────────────────────────────

1. Aller sur https://vercel.com/signup
2. Se connecter avec :
   - GitHub (nouveau compte)
   - Ou Email
   - Ou Google


6.2. IMPORTER LE PROJET
───────────────────────

1. Dashboard Vercel > "Add New..." > "Project"
2. "Import Git Repository"
3. Autoriser l'accès à votre nouveau compte GitHub
4. Sélectionner `mon-nouveau-projet`


6.3. CONFIGURER LE PROJET
──────────────────────────

**Framework Preset** : Vite (ou React, Next.js selon votre projet)

**Root Directory** : 
  - Cliquer "Edit"
  - Sélectionner `frontend` (si votre frontend est dans un sous-dossier)

**Build Settings** :
  - Build Command : `npm run build` (ou `vite build`)
  - Output Directory : `dist` (ou `build`)
  - Install Command : `npm install`


6.4. AJOUTER LES VARIABLES D'ENVIRONNEMENT
───────────────────────────────────────────

1. Section "Environment Variables"
2. Ajouter :

```env
VITE_API_URL=https://votre-backend.up.railway.app
```

💡 **Important** : Remplacer par l'URL Railway de la Phase 5.4


6.5. DÉPLOYER
─────────────

1. Cliquer "Deploy"
2. Attendre 1-2 minutes
3. ✅ Vercel affiche "Congratulations!" avec l'URL


6.6. OBTENIR L'URL DU FRONTEND
───────────────────────────────

Vercel génère automatiquement :
```
https://mon-nouveau-projet.vercel.app
```

📝 **C'est l'URL finale de votre application !**


6.7. CONFIGURER LE DOMAINE PERSONNALISÉ (Optionnel)
────────────────────────────────────────────────────

1. Onglet "Settings" > "Domains"
2. Ajouter votre domaine personnel
3. Suivre les instructions DNS


┌──────────────────────────────────────────────────────────────────────────────┐
│ PHASE 7 : CONFIGURER CORS ET FINALISER (5 minutes)                           │
└──────────────────────────────────────────────────────────────────────────────┘

7.1. METTRE À JOUR LE CORS BACKEND
───────────────────────────────────

Dans `backend/server.js` (ou votre fichier principal) :

```javascript
import cors from 'cors';

app.use(cors({
  origin: [
    'http://localhost:5173',              // Dev local
    'https://mon-nouveau-projet.vercel.app',  // Vercel
  ],
  credentials: true
}));
```


7.2. POUSSER LES CHANGEMENTS
─────────────────────────────

```bash
git add .
git commit -m "Configure CORS for production"
git push origin main
```

Railway redéploie automatiquement !


7.3. RECONFIGURER LE SYSTÈME DE GÉOLOCALISATION (Si applicable)
────────────────────────────────────────────────────────────────

Si vous avez le système de géolocalisation :

```bash
# Mettre à jour les coordonnées GPS dans scripts/setup-store-location.js
node scripts/setup-store-location.js
```


┌──────────────────────────────────────────────────────────────────────────────┐
│ PHASE 8 : TESTS FINAUX (5 minutes)                                           │
└──────────────────────────────────────────────────────────────────────────────┘

8.1. TESTER EN LOCAL
────────────────────

```bash
# Terminal 1 : Backend
npm run dev
# Devrait démarrer sur http://localhost:3000

# Terminal 2 : Frontend
cd frontend
npm run dev
# Devrait démarrer sur http://localhost:5173
```

Ouvrir http://localhost:5173
✅ Vérifier :
  - Page d'accueil s'affiche
  - Connexion fonctionne
  - API répond


8.2. TESTER EN PRODUCTION
──────────────────────────

Ouvrir `https://mon-nouveau-projet.vercel.app`

✅ Checklist :
  - [ ] Page d'accueil se charge
  - [ ] Pas d'erreurs dans la console (F12)
  - [ ] Connexion fonctionne
  - [ ] Requêtes API passent (vérifier Network tab)
  - [ ] Toutes les fonctionnalités marchent


8.3. VÉRIFIER LES LOGS
──────────────────────

**Railway** :
  - Aller sur railway.app
  - Votre projet > Backend service
  - Onglet "Logs"
  - ✅ Pas d'erreurs rouges

**Vercel** :
  - Aller sur vercel.com
  - Votre projet > Deployments
  - Cliquer sur le dernier déploiement
  - Onglet "Logs"


═══════════════════════════════════════════════════════════════════════════════
                      📋 CHECKLIST COMPLÈTE DE DUPLICATION
═══════════════════════════════════════════════════════════════════════════════

PRÉPARATION
 ☐ Projet nettoyé (node_modules supprimés)
 ☐ Variables d'environnement sauvegardées
 ☐ Archive créée (.zip ou .tar.gz)
 ☐ Archive transférée sur le nouvel ordinateur

NOUVEL ORDINATEUR
 ☐ Node.js installé (node --version)
 ☐ Git installé (git --version)
 ☐ Projet extrait
 ☐ Dépendances installées (npm install)

GITHUB
 ☐ Nouveau compte créé (si nécessaire)
 ☐ Nouveau dépôt créé
 ☐ Code poussé sur GitHub
 ☐ Dépôt accessible en ligne

RAILWAY
 ☐ Nouveau compte créé (si nécessaire)
 ☐ Nouveau projet créé
 ☐ Base de données PostgreSQL créée
 ☐ DATABASE_URL récupérée
 ☐ Migrations Prisma appliquées
 ☐ Utilisateur admin créé
 ☐ Backend déployé
 ☐ URL backend récupérée

VERCEL
 ☐ Nouveau compte créé (si nécessaire)
 ☐ Projet importé depuis GitHub
 ☐ Variables d'environnement configurées
 ☐ Frontend déployé
 ☐ URL frontend récupérée

CONFIGURATION FINALE
 ☐ CORS configuré dans le backend
 ☐ Changements poussés sur GitHub
 ☐ Tests locaux réussis
 ☐ Tests production réussis
 ☐ Pas d'erreurs dans les logs


═══════════════════════════════════════════════════════════════════════════════
                         🔧 RÉSOLUTION DE PROBLÈMES COURANTS
═══════════════════════════════════════════════════════════════════════════════

PROBLÈME 1 : "npm install" échoue
──────────────────────────────────
Causes possibles :
  - Versions de Node.js incompatibles
  - package-lock.json corrompu
  
Solutions :
```bash
# Vérifier la version Node.js (doit être 18+)
node --version

# Nettoyer et réinstaller
rm -rf node_modules package-lock.json
npm install

# Ou utiliser --legacy-peer-deps
npm install --legacy-peer-deps
```


PROBLÈME 2 : Erreur "Prisma Client not generated"
──────────────────────────────────────────────────
Solution :
```bash
npx prisma generate
npx prisma migrate deploy
```


PROBLÈME 3 : Erreur CORS en production
───────────────────────────────────────
Vérifier dans `server.js` :
```javascript
app.use(cors({
  origin: [
    'https://mon-nouveau-projet.vercel.app',  // ⚠️ Votre vraie URL
  ],
  credentials: true
}));
```

Redéployer :
```bash
git add .
git commit -m "Fix CORS"
git push
```


PROBLÈME 4 : "Cannot connect to database"
──────────────────────────────────────────
Vérifier :
  1. DATABASE_URL est correcte dans Railway variables
  2. Format : `postgresql://user:password@host:5432/database`
  3. Pas d'espaces ou guillemets en trop


PROBLÈME 5 : Frontend ne peut pas joindre le backend
─────────────────────────────────────────────────────
Vérifier :
  1. VITE_API_URL dans Vercel pointe vers l'URL Railway
  2. Pas de "http" si Railway utilise "https"
  3. Pas de "/" à la fin de l'URL


PROBLÈME 6 : "Authentication failed" sur GitHub
────────────────────────────────────────────────
GitHub n'accepte plus les mots de passe depuis 2021.

Solution : Utiliser un Personal Access Token
  1. GitHub > Settings > Developer settings
  2. Personal access tokens > Tokens (classic)
  3. Generate new token
  4. Cocher "repo"
  5. Copier le token
  6. Utiliser comme mot de passe lors du `git push`


PROBLÈME 7 : Railway deploy échoue
───────────────────────────────────
Vérifier :
  - Root Directory est correct
  - Start Command est correct (`npm start` ou `node server.js`)
  - package.json contient le script "start"
  - Toutes les variables d'environnement sont configurées


PROBLÈME 8 : Vercel build échoue
─────────────────────────────────
Causes communes :
  - Root Directory incorrect (mettre `frontend`)
  - VITE_API_URL manquante dans les variables
  - Erreurs TypeScript (si strict)

Solution :
  1. Vérifier les logs de build sur Vercel
  2. Corriger les erreurs localement
  3. Pousser sur GitHub


═══════════════════════════════════════════════════════════════════════════════
                         💡 ASTUCES ET BONNES PRATIQUES
═══════════════════════════════════════════════════════════════════════════════

1. SÉCURITÉ
───────────
✅ Ne jamais commiter les .env sur GitHub
✅ Ajouter .env dans .gitignore
✅ Utiliser des secrets forts pour JWT_SECRET
✅ Créer des comptes différents pour chaque environnement

Exemple de .gitignore :
```
node_modules/
dist/
.env
.env.local
.env.production
*.log
.DS_Store
```


2. ORGANISATION
───────────────
📁 Structure recommandée :
```
mon-projet/
├── backend/
│   ├── prisma/
│   ├── routes/
│   ├── .env
│   ├── server.js
│   └── package.json
├── frontend/
│   ├── src/
│   ├── .env
│   └── package.json
├── .gitignore
└── README.md
```


3. DOCUMENTATION
────────────────
Créer un README.md dans votre projet :

```markdown
# Mon Nouveau Projet

## URLs
- Frontend : https://mon-nouveau-projet.vercel.app
- Backend : https://backend.railway.app
- GitHub : https://github.com/username/mon-nouveau-projet

## Installation locale
\`\`\`bash
npm install
cd frontend && npm install
\`\`\`

## Lancer en local
\`\`\`bash
# Backend
npm run dev

# Frontend
cd frontend
npm run dev
\`\`\`

## Variables d'environnement
Voir ENV_BACKUP.txt
```


4. BACKUP RÉGULIERS
───────────────────
Créer un script de backup :

**scripts/backup.sh**
```bash
#!/bin/bash
DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_DIR="backups"

mkdir -p $BACKUP_DIR

# Backup code
git archive -o "$BACKUP_DIR/code_$DATE.zip" HEAD

# Backup database
npx prisma db pull

echo "✅ Backup créé : $BACKUP_DIR/code_$DATE.zip"
```


5. ENVIRONNEMENTS MULTIPLES
────────────────────────────
Créer plusieurs fichiers .env :

- `.env.development` : Pour le dev local
- `.env.staging` : Pour un environnement de test
- `.env.production` : Pour la production

Dans package.json :
```json
{
  "scripts": {
    "dev": "NODE_ENV=development node server.js",
    "start": "NODE_ENV=production node server.js"
  }
}
```


6. MONITORING
─────────────
Après déploiement, surveiller :
  - Railway Logs : Erreurs backend
  - Vercel Analytics : Performance frontend
  - Railway Metrics : CPU, RAM, requêtes DB


═══════════════════════════════════════════════════════════════════════════════
                         🎯 CAS D'USAGE SPÉCIFIQUES
═══════════════════════════════════════════════════════════════════════════════

CAS 1 : Dupliquer pour un CLIENT
─────────────────────────────────
1. Créer un compte GitHub pour le client
2. Créer des comptes Railway/Vercel au nom du client
3. Personnaliser :
   - Nom du projet
   - Logo et couleurs
   - Coordonnées GPS (si géolocalisation)
   - Messages et textes
4. Former le client à utiliser les dashboards


CAS 2 : Dupliquer pour TESTER
──────────────────────────────
1. Utiliser vos comptes existants
2. Nommer le projet "projet-test" ou "projet-staging"
3. Utiliser une DB de test (pas de données réelles)
4. Tester les nouvelles fonctionnalités
5. Merger dans le projet principal si OK


CAS 3 : Dupliquer pour un AUTRE DÉVELOPPEUR
────────────────────────────────────────────
1. Créer un dépôt GitHub en tant qu'organisation
2. Inviter le développeur comme collaborateur
3. Chacun a sa propre DB de dev en local
4. Partager une DB Railway pour la prod
5. Utiliser des branches Git pour collaborer


CAS 4 : Dupliquer vers un AUTRE PAYS
─────────────────────────────────────
Considérations :
  - Railway : Choisir la région (EU, US, Asia)
  - Vercel : Choisir la région de déploiement
  - Base de données : Considérer la latence
  - Traductions : Adapter les textes
  - Devise : Adapter si e-commerce
  - Fuseau horaire : Gérer correctement les dates


═══════════════════════════════════════════════════════════════════════════════
                         📊 TABLEAU DE SUIVI DES PROJETS
═══════════════════════════════════════════════════════════════════════════════

Créer un fichier PROJETS.md pour suivre vos duplications :

```markdown
# Mes Projets Dupliqués

## Projet 1 : Client ABC
- **GitHub** : https://github.com/abc/projet-abc
- **Railway** : https://railway.app/project/xxx
- **Vercel** : https://projet-abc.vercel.app
- **Date création** : 2026-02-01
- **Statut** : ✅ Production
- **Notes** : Géolocalisation activée

## Projet 2 : Test Features
- **GitHub** : https://github.com/test/projet-test
- **Railway** : https://railway.app/project/yyy
- **Vercel** : https://projet-test.vercel.app
- **Date création** : 2026-02-05
- **Statut** : 🚧 En cours
- **Notes** : Tests nouvelles fonctionnalités

## Projet Original
- **GitHub** : https://github.com/original/gs-pipeline
- **Railway** : https://railway.app/project/zzz
- **Vercel** : https://gs-pipeline.vercel.app
- **Date création** : 2026-01-15
- **Statut** : ✅ Production principale
- **Notes** : Référence principale
```


═══════════════════════════════════════════════════════════════════════════════
                         🔐 GESTION DES COMPTES MULTIPLES
═══════════════════════════════════════════════════════════════════════════════

GITHUB - Comptes Multiples
──────────────────────────

**Méthode 1 : SSH Keys différentes**
```bash
# Générer une clé SSH pour le nouveau compte
ssh-keygen -t ed25519 -C "nouveau@email.com" -f ~/.ssh/id_ed25519_nouveau

# Ajouter au ssh-agent
ssh-add ~/.ssh/id_ed25519_nouveau

# Configurer dans ~/.ssh/config
Host github-nouveau
  HostName github.com
  User git
  IdentityFile ~/.ssh/id_ed25519_nouveau

# Utiliser dans git remote
git remote add origin git@github-nouveau:username/repo.git
```

**Méthode 2 : Git config par dossier**
```bash
# Dans le dossier du projet
git config user.name "Nouveau Nom"
git config user.email "nouveau@email.com"
```


RAILWAY - Comptes Multiples
────────────────────────────
1. Utiliser des emails différents
2. Se déconnecter/reconnecter selon le projet
3. Utiliser des navigateurs différents (Chrome, Firefox)
4. Ou mode incognito pour un compte, mode normal pour l'autre


VERCEL - Comptes Multiples
───────────────────────────
1. Utiliser des emails différents
2. Se déconnecter/reconnecter
3. Ou créer des "Teams" dans un seul compte


═══════════════════════════════════════════════════════════════════════════════
                         💰 COÛTS ET LIMITATIONS GRATUITES
═══════════════════════════════════════════════════════════════════════════════

GITHUB (Gratuit illimité)
─────────────────────────
✅ Dépôts publics illimités
✅ Dépôts privés illimités
✅ Actions (2000 minutes/mois)
❌ Limite : GitHub Pages (1 site par dépôt)


RAILWAY (Plan Gratuit)
──────────────────────
✅ 500 heures d'exécution/mois
✅ 1 GB RAM
✅ 1 GB stockage DB
✅ 100 GB bande passante/mois
⚠️ Suffisant pour 1-2 petits projets
💰 Plan Hobby : $5/mois pour plus


VERCEL (Plan Gratuit)
─────────────────────
✅ Déploiements illimités
✅ 100 GB bande passante/mois
✅ Builds illimités
✅ Domaines personnalisés
✅ SSL automatique
⚠️ Suffisant pour plusieurs projets
💰 Plan Pro : $20/mois par équipe


ALTERNATIVE : Hébergement Gratuit
──────────────────────────────────
Si vous dépassez les limites gratuites :

**Backend** :
  - Render.com (750h/mois gratuit)
  - Fly.io (3 apps gratuites)
  - Heroku (hobby dynos)

**Base de données** :
  - Neon.tech (PostgreSQL gratuit)
  - PlanetScale (MySQL gratuit)
  - Supabase (PostgreSQL gratuit)

**Frontend** :
  - Netlify (100 GB/mois)
  - Cloudflare Pages (illimité)
  - GitHub Pages (gratuit)


═══════════════════════════════════════════════════════════════════════════════
                         ⚡ AUTOMATISATION AVANCÉE
═══════════════════════════════════════════════════════════════════════════════

SCRIPT DE DUPLICATION AUTOMATIQUE
──────────────────────────────────

Créer `duplicate-project.sh` :

```bash
#!/bin/bash

# Configuration
NEW_PROJECT_NAME="mon-nouveau-projet"
NEW_GITHUB_USER="nouveau-username"
NEW_GITHUB_REPO="$NEW_PROJECT_NAME"

echo "🚀 Duplication du projet : $NEW_PROJECT_NAME"

# 1. Nettoyer
echo "🧹 Nettoyage..."
rm -rf node_modules frontend/node_modules dist frontend/dist

# 2. Créer une copie
echo "📦 Création de la copie..."
cd ..
cp -r mon-projet-original $NEW_PROJECT_NAME
cd $NEW_PROJECT_NAME

# 3. Initialiser Git
echo "🔧 Initialisation Git..."
rm -rf .git
git init
git add .
git commit -m "Initial commit"

# 4. Créer le dépôt GitHub (nécessite GitHub CLI)
echo "🐙 Création du dépôt GitHub..."
gh repo create $NEW_GITHUB_REPO --private --source=. --remote=origin --push

echo "✅ Duplication terminée !"
echo "📝 Prochaines étapes :"
echo "  1. Configurer Railway"
echo "  2. Configurer Vercel"
echo "  3. Mettre à jour les variables d'environnement"
```

Rendre exécutable :
```bash
chmod +x duplicate-project.sh
./duplicate-project.sh
```


GitHub CLI (pour automatisation)
─────────────────────────────────
Installer : https://cli.github.com

```bash
# Installer
# Windows : winget install GitHub.cli
# Mac : brew install gh
# Linux : voir https://cli.github.com

# Se connecter
gh auth login

# Créer un dépôt
gh repo create mon-projet --private

# Pousser
git push -u origin main
```


═══════════════════════════════════════════════════════════════════════════════
                         🎉 FÉLICITATIONS !
═══════════════════════════════════════════════════════════════════════════════

Vous savez maintenant comment :
✅ Dupliquer intégralement un projet
✅ Configurer GitHub, Railway et Vercel avec de nouveaux comptes
✅ Gérer plusieurs projets en parallèle
✅ Résoudre les problèmes courants
✅ Automatiser le processus

Votre nouveau projet est maintenant :
🚀 Déployé en production
🔒 Sécurisé avec de nouveaux identifiants
📊 Surveillé via les dashboards
💾 Sauvegardé sur GitHub

N'hésitez pas à :
📖 Documenter chaque projet dans PROJETS.md
🔄 Créer des backups réguliers
🧪 Tester avant de partager
👥 Former vos clients/collaborateurs


═══════════════════════════════════════════════════════════════════════════════
                              📞 AIDE RAPIDE
═══════════════════════════════════════════════════════════════════════════════

Commandes essentielles à retenir :

```bash
# Installation
npm install

# Migrations DB
npx prisma generate
npx prisma migrate deploy

# Lancer en local
npm run dev

# Git
git add .
git commit -m "message"
git push origin main

# Prisma Studio
npx prisma studio
```

Liens utiles :
- GitHub : https://github.com
- Railway : https://railway.app
- Vercel : https://vercel.com
- Documentation Prisma : https://prisma.io/docs


═══════════════════════════════════════════════════════════════════════════════

                    Dernière mise à jour : 1er février 2026
                    Version : 1.0
                    Créé avec Cursor AI

═══════════════════════════════════════════════════════════════════════════════

                        🚀 BONNE DUPLICATION ! 🚀

═══════════════════════════════════════════════════════════════════════════════









